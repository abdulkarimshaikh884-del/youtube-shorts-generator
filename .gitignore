node_modules/
.env
.env.local
# .env.example carries no secrets — it is the template a deploy is set up from,
# so it belongs in the repo.
!.env.example
data/
*.log
*.ps1
shots/
shot.js
verify_*.js
# The mobile journey is a release regression, not a disposable audit script.
!verify_mobile.js
diag_studio.js
DEPENDENCY_REPORT.md

.credits.json
.credits.json.tmp
.users.json
.users.json.tmp
.community.json
audit_results/
audit_*.js
.waitlist.json

# Audit/QA scratch scripts and screenshots
_*.js
_shots/
